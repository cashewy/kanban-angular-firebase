Remind steps to upload project to github
1.create github repo
2.in local project "git init"
3. git add .
4. git commit -m "some comment if you want"
5. git remote add origin {link to github repot ex:"https://github.com/cashewy/kanban-angular-firebase.git"}
6.git push -u -f origin master
